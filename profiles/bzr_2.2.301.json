{
  "schema_version": 1,
  "profile_id": "bzr-2.2.301",
  "game": "Battlezone 98 Redux",
  "version": "2.2.301",
  "executable": "bzr.exe",
  "architecture": "x86",
  "image_base": "0x00400000",
  "catalog": "../exu.json",
  "notes": "Runtime-gate anchors are intentionally conservative and cover more than one subsystem. Offline qualification also reports optional signatures that are useful for diagnosing a new build without making them hard requirements for normal startup.",
  "anchors": [
    {
      "name": "Camera.Set_View",
      "source": "catalog",
      "catalog_path": "Camera.Set_View",
      "match": "expected_va",
      "expected_va": "0x0061D120",
      "required": false,
      "runtime_gate": false,
      "reason": "Legacy catalog diagnostic retained for camera-address repair; the shipped 2.2.301 executable does not match this stale preimage, so it must not disable unrelated native hooks."
    },
    {
      "name": "Camera.View_Record_MainCam reference",
      "source": "catalog",
      "catalog_path": "Camera.View_Record_MainCam",
      "match": "executable_contains",
      "required": false,
      "runtime_gate": false,
      "reason": "Legacy catalog diagnostic retained for camera-address repair; the shipped 2.2.301 executable does not contain this stale reference pattern."
    },
    {
      "name": "Camera.zoomFactorFPP reference",
      "source": "catalog",
      "catalog_path": "Camera.zoomFactorFPP",
      "match": "executable_contains",
      "required": false,
      "runtime_gate": false,
      "reason": "Legacy catalog diagnostic retained for camera-address repair; the shipped 2.2.301 executable does not contain this stale reference pattern."
    },
    {
      "name": "Overlay pause wrapper",
      "source": "inline",
      "pattern": "55 8B EC 51 83 3D 2C 83 91 00 00 74 05 E9 ? ? ? ? 0F B6 05 2B 81 91 00 85 C0 0F 85 ? ? ? ?",
      "match": "expected_va",
      "expected_va": "0x005D4690",
      "required": true,
      "runtime_gate": true,
      "source_file": "src/UI/Overlay.cpp",
      "reason": "Existing overlay hook preimage from a separate BZR subsystem; used here to avoid a single-anchor build identity."
    },
    {
      "name": "Overlay game shell wrapper",
      "source": "catalog",
      "catalog_path": "GameUI.MainShellWrapper",
      "match": "expected_va",
      "expected_va": "0x005D42E0",
      "required": true,
      "runtime_gate": true,
      "reason": "Validates the long-lived shell boundary that suppresses custom overlays on mission-success, mission-failure, and all other stock shell pages."
    },
    {
      "name": "Wingman Hunt activation",
      "source": "inline",
      "pattern": "83 7D 08 0D 75 10 6A 14 8B 4D FC E8 ? ? ? ? B0 01 EB ? EB ? 83 7D 08 10 75 ? A1 ? ? ? ? 50 6A 06 8B 4D FC E8 ? ? ? ?",
      "match": "unique_executable",
      "required": true,
      "runtime_gate": true,
      "source_file": "src/Game/CommandReplacement.cpp",
      "reason": "Unique, independently validated 2.2.301 gameplay anchor outside the UI subsystem; prevents the overlay wrappers alone from becoming the entire global native-patch gate."
    }
  ]
}
